name: Validate PF1e data
on:
  push:
  pull_request:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Check
        run: npm run check > check.log 2>&1
      - name: Upload final Sorcerer failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: final-sorcerer-chassis-failure
          path: check.log
      - run: npm run build
