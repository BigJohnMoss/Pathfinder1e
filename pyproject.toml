[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pathfinder1e"
version = "0.1.0"
description = "Minimal scaffold for Pathfinder1e"
requires-python = ">=3.8"
authors = [ { name = "Your Name", email = "you@example.com" } ]

[tool]

