import assert from "node:assert/strict";import test from "node:test";import {readFile} from "node:fs/promises";import spells from "../generated/pf1e-spells.mjs";
import {apgClassResourceMaximums,classProgression,eidolonBaseForm,eidolonEvolutionPool,spellsAvailableToClass,validateEidolonEvolutions} from "../packages/engine/src/index.js";
import {spontaneousSpellcastingProgression} from "../packages/engine/src/spontaneous-spellcasting.js";
const summoner=JSON.parse(await readFile(new URL("../packages/data/src/classes/summoner.json",import.meta.url),"utf8"));const catalogue=JSON.parse(await readFile(new URL("../packages/data/src/options/eidolon-evolutions.json",import.meta.url),"utf8"));
test("Summoner reaches level 20 with its complete bond chassis",()=>{const p=classProgression(summoner,20);assert.equal(p.baseAttackBonus,15);assert.ok(p.features.some(f=>f.id==="summoner-twin-eidolon-20"));assert.deepEqual(apgClassResourceMaximums("summoner",20,{charisma:5}),{summonMonster:8,bondSensesRounds:20,makersCall:4});});
test("Summoner casts six levels from its imported list",()=>{assert.equal(spontaneousSpellcastingProgression(summoner,20,{abilityScore:20}).maximumSpellLevel,6);assert.ok(spellsAvailableToClass(spells,"summoner",6).length>=375);});
test("Eidolon forms and evolution budgets enforce form, level, prerequisites, and cost",()=>{assert.equal(eidolonBaseForm("biped").abilities.strength,16);assert.equal(eidolonEvolutionPool(20),26);const result=validateEidolonEvolutions(["evolution-tail-slap","evolution-tail","evolution-tail-slap","evolution-large"],catalogue.options,7,"quadruped");assert.deepEqual(result.selectedIds,["evolution-tail","evolution-tail-slap"]);assert.equal(result.spent,2);});
